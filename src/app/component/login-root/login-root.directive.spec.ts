import { LoginRootDirective } from './login-root.directive'

describe('LoginRootDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginRootDirective(null as any)
    expect(directive).toBeTruthy()
  })
})
